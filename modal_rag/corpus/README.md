# Corpus RAG

I file di questa cartella si generano **dopo** aver configurato il comune
(`istat_code` reale in `config/comune.json`):

```bash
npm run rag:corpus
```

Nel template non sono versionati dati di un ente specifico.
Vedi `modal_rag/README.md`.
