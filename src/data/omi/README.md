# Snapshot OMI

Nessun file è committato nel template (non appartiene a un comune).

Dopo aver compilato `istat_code` in `config/comune.json`:

```bash
npm run omi:update
```

Viene creato `src/data/omi/{ISTAT}.json`. Attribuzione: Agenzia delle Entrate – OMI.
